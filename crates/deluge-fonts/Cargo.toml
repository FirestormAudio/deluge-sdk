[package]
name = "embedded-fonts-deluge"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"

[features]
default = ["embedded-graphics"]
embedded-graphics = ["dep:embedded-graphics"]

[dependencies]
embedded-graphics = { version = "0.8", optional = true }

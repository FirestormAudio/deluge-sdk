[package]
publish = false  # binary crate — not for crates.io
name = "midi_cv"
version = "0.1.0"
edition = "2024"
license.workspace = true
description = "Deluge SDK example: MIDI DIN -> CV/gate converter."

[[bin]]
name = "midi_cv"
test = false

[dependencies]
deluge = { path = "../../crates/deluge-sdk", package = "deluge-sdk" }

[features]
default = []
## Opt-in RTT (SEGGER) logging over a debug probe.
rtt = ["deluge/rtt"]

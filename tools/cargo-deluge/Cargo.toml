[package]
name = "cargo-deluge"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

[[bin]]
name = "cargo-deluge"
path = "src/main.rs"

[dependencies]
# Intentionally none — pure std so it builds offline with no supply chain.
